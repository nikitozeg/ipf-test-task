# OS / editor
.DS_Store
.idea/
*.log

# Node
node_modules/
npm-debug.log

# k6 generated reports & data (regenerated by running the tests)
summary.json
*-summary.json
*.html
*.ndjson
*.ndjson.gz

# Terraform — never commit local state or the provider cache.
# (.terraform.lock.hcl IS committed — it pins provider versions.)
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
crash.log
